@model Practica_new.Models.Disk

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Disk</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdDisk" />
            <div class="form-group">
                <label asp-for="IdSsd" class="control-label"></label>
                <select asp-for="IdSsd" class="form-control" asp-items="ViewBag.IdSsd"></select>
                <span asp-validation-for="IdSsd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdHdd" class="control-label"></label>
                <select asp-for="IdHdd" class="form-control" asp-items="ViewBag.IdHdd"></select>
                <span asp-validation-for="IdHdd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
