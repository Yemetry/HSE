@model IEnumerable<Practica_new.Models.Disk>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdHddNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSsdNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdHddNavigation.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSsdNavigation.Brand)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDisk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDisk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDisk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
